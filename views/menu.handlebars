{{#if loggedIn}}
<section class="menu">
  <img src="/images/{{}}" alt="{{}}">
  <div class="menu-details">
    <h3>{{menu.title}}</h3>
  </div>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}
